package
{
   import flash.display.BitmapData;
   
   public dynamic class UnsafeBlock extends BitmapData
   {
      public function UnsafeBlock(width:int = 30, height:int = 30)
      {
         super(width,height);
      }
   }
}
